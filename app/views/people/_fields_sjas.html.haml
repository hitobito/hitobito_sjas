-#  Copyright (c) 2012-2020, Stiftung für junge Auslandschweizer. This file is part of
-#  hitobito_sjas and licensed under the Affero General Public License version 3
-#  or later. See the COPYING file at the top-level directory or at
-#  https://github.com/hitobito/hitobito_sjas.

-# BEWARE: This partial is used from different places (e.g. event/register/register).
-#         Make sure not to use relative partial paths here or in any extensions
-#         unless these partials also exist in all folders referencing this file here.

= field_set_tag do
  = f.labeled_input_fields :place_of_origin

= field_set_tag do
  - %w(de fr it en es).each do |l|
    = f.labeled("language_skill_#{l}") do
      - possible_person_language_skills.each do |key, value|
        = f.inline_radio_button("language_skill_#{l}", key, value)
  = f.labeled_input_fields :other_native_language, class: 'mt-3'
